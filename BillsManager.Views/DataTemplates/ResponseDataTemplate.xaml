<UserControl x:Class="BillsManager.View.DataTemplates.ResponseDataTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Micro="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
             mc:Ignorable="d"
             d:DesignHeight="25"
             d:DesignWidth="80">

    <UserControl.Resources>
        <ResourceDictionary Source="/BillsManager.View;component/Dictionaries/Styles.xaml" />
    </UserControl.Resources>

    <Button Width="80"
            Margin="8 0 0 0"
            Content="{Binding Path=Text}"
            IsCancel="{Binding Path=IsCancel}"
            IsDefault="{Binding Path=IsDefault}"
            IsEnabled="{Binding Path=IsEnabled}"
            Micro:Message.Attach="Respond($dataContext)" />
</UserControl>
