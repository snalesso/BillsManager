using System;

namespace Billy.Domain.Persistence.SQL.SQLite3.Dapper
{
    [Obsolete("Old, unused. Not deleted just to use it as a snippet.")]
    internal static class DapperSQLite3QueryHelper
    {
        //public static string GetParameteredInsert<TTable, TInsert>(string tableName, IEnumerable<SQLite3ColumnDef<T>> columnDefs)
        //{
        //    return $"INSERT INTO {TTable.GetType().Name }"
        //}
    }
}