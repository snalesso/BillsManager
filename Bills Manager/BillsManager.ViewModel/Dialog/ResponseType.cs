namespace BillsManager.ViewModels
{
    public enum ResponseType
    {
        InvalidResponse = -1,
        No,
        Yes,
        Cancel,
        Ok,
        Abort,
        Retry
    }
}