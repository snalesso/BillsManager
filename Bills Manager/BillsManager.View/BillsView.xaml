<UserControl x:Class="BillsManager.View.BillsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ViewModel="clr-namespace:BillsManager.ViewModel;assembly=BillsManager.ViewModel"
             xmlns:DataTemplates="clr-namespace:BillsManager.View.DataTemplates"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=ViewModel:BillsViewModel, IsDesignTimeCreatable=True}">

    <UserControl.Resources>
        <ResourceDictionary Source="Dictionaries\Styles.xaml" />
    </UserControl.Resources>

    <ListBox Grid.IsSharedSizeScope="True"
             ItemsSource="{Binding FilteredBillViewModels, Mode=OneWay}"
             MinHeight="350"
             SelectedItem="{Binding Path=SelectedBillViewModel}"
             SelectionMode="Single"
             BorderThickness="0">

        <ListBox.ItemTemplate>
            <DataTemplate>
                <DataTemplates:BillDataTemplate />
            </DataTemplate>
        </ListBox.ItemTemplate>

        <ListBox.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Add"
                          Command="{Binding Path=AddNewBillCommand, Mode=OneTime}" />
                <Separator />
                <MenuItem Header="Edit"
                          Command="{Binding Path=EditBillCommand, Mode=OneTime}"
                          CommandParameter="{Binding SelectedBillViewModel}" />
                <MenuItem Header="Delete"
                          Command="{Binding Path=DeleteBillCommand, Mode=OneTime}"
                          CommandParameter="{Binding SelectedBillViewModel}" />
                <Separator />
                <MenuItem Header="Pay"
                          Command="{Binding Path=PayBillCommand, Mode=OneTime}"
                          CommandParameter="{Binding SelectedBillViewModel}" />
            </ContextMenu>
        </ListBox.ContextMenu>

    </ListBox>

</UserControl>