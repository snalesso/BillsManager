<UserControl x:Class="BillsManager.View.BillsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ViewModel="clr-namespace:BillsManager.ViewModel;assembly=BillsManager.ViewModel"
             xmlns:DataTemplates="clr-namespace:BillsManager.View.DataTemplates"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=ViewModel:BillsViewModel, IsDesignTimeCreatable=True}">
    
    <UserControl.Resources>
        <ResourceDictionary Source="Dictionaries\Styles.xaml" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <ContentControl x:Name="SearchBillsViewModel" Margin="8 0 0 0"/>

        <ListBox Grid.IsSharedSizeScope="True"
                 ItemsSource="{Binding Path=FilteredBillViewModels}"
                 MinHeight="350"
                 SelectedItem="{Binding Path=SelectedBillViewModel}"
                 SelectionMode="Single"
                 BorderThickness="0"
                 Grid.Row="2">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DataTemplates:BillDataTemplate />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!--<ListView Grid.Column="1" Grid.Row="3" MinHeight="350"
                  ItemsSource="{Binding Path=BillViewModels}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Code}"
                                    Header="Code" />
                    <GridViewColumn Header="Amount"
                                    DisplayMemberBinding="{Binding Path=Amount}" />
                    <GridViewColumn Header="Supplier"
                                    DisplayMemberBinding="{Binding Path=Supplier.Name}" />
                    <GridViewColumn DisplayMemberBinding="{Binding Path=DueDate}"
                                    Header="Due date" />
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Notes}"
                                    Header="Notes" />
                    <GridViewColumn DisplayMemberBinding="{Binding Path=PaymentDate}"
                                    Header="Payed" />
                </GridView>
            </ListView.View>
        </ListView>-->
    </Grid>

</UserControl>